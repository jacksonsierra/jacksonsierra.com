'use strict';

describe('jacksonSierra.apps module', function() {

  beforeEach(module('jacksonSierra.apps'));

  describe('apps controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var appsCtrl = $controller('AppsCtrl');
      expect(appsCtrl).toBeDefined();
    }));

  });
});